# AGMCIS — 交易系統(Claude Code 必讀)

紙上交易系統(paper-only,永不實盤下單)。操作者無程式背景,
以 iPhone + Termius SSH 操作,全程繁體中文。你的角色是工程執行者,
執政官(使用者)保留一切裁決權。

**2026-09-08 全系統重建。** 舊系統(法庭 + 影子 + 十積木 + 十官)
已完整刪除,git tag `v1-legacy-full` 保留最後快照。
刪除的理由與教訓寫在本檔第六節 —— 那些教訓比程式碼值錢。

## 一、系統長什麼樣

```
訊號   收盤站上 50 日均線才持有          portfolio/rules.py
規模   波動目標 27%(回看 50 日)         portfolio/rules.py
訂單   買什麼、多少、出場線在哪            portfolio/orders.py
執行   紙上 / 實盤(三道硬鎖)            portfolio/execution.py
帳本   **合約規格**(BingX 欄位對齊)        portfolio/account.py
槓桿   波動公式浮動,上限 3×(2026-09-10 由 20× 下修)  portfolio/rules.py
監控   行為還在歷史範圍內嗎               portfolio/monitor.py
契約   實盤資格八條                      portfolio/contract.py
串流   交易所 WebSocket 逐筆推播          portfolio/stream.py
即時   盤中盈虧(只讀不寫)                portfolio/live.py
帳戶   交易所私有端點(唯讀)              exchange/bingx/private.py
相關   七個倉是不是同一個賭注              portfolio/correlation.py
事件   今天是什麼日子(只記錄)              portfolio/events.py
限流   跨行程令牌桶                       core/ratelimit.py
健康   /health,機器問得到                core/health.py
```

**帳本是合約規格,不是現貨規格(2026-09-08)**:
合約不是「拿 USDT 去買幣」,而是**鎖一部分當保證金**,USDT 從頭到尾是 USDT。
1× 槓桿下權益數字相同,但現貨式記法讓三個欄位無法存在,
而它們是合約交易員最先看的:**可用保證金 / 強平價 / 保證金率**。
欄位對齊 BingX Swap V2(CCXT 對接規格):
`balance / equity / unrealizedProfit / usedMargin / availableMargin`、
`positionAmt`(正=多 負=空)`/ avgPrice / markPrice / liquidationPrice /
initialMargin / maintenanceMargin / marginRatio / leverage`。
**強平以標記價判定**(交易所就是這樣做的;用最新價會被插針掃掉)。
維持保證金率 0.5%(API 沒回這欄位,取偏高一側:假設要往不利自己的方向錯)。
`liquidation_check()` 存在的理由:「理論上不會發生而沒有程式碼守著」的事,
發生時沒人會知道。**2026-09-10 它真的發生了** —— 見第四節。
遷移驗證:權益 9,996.536867 前後完全相同,**前向樣本未斷**。

**記帳權益 vs 即時權益**:記帳每日一次(訊號用收盤、成交在隔日開盤)
—— 那是策略的節奏。即時每 10 秒 —— 那只給人看,**不進任何決策、
不寫任何帳本**。讓即時價格進到決策裡,策略就從日線變成盯盤,
而回測的 Calmar 1.33 是在日線規則下算出來的。
**真正的即時**:BingX WebSocket 逐筆推播(實測約 10 筆/秒)→ 本進程記憶體
→ SSE 推到瀏覽器,**全程無輪詢**。面板每秒收一筆快照(再快人眼讀不了,
數字也會抖到看不清)。REST 只是串流未連上時的退路,串流的價格永遠優先。
斷線期間**明確標記為過期** —— 一條靜止不動卻標著「即時」的價格,
比沒有價格危險。

`tests/test_live.py` 斷言:即時層與串流層都無任何寫入路徑、
決策路徑(rules/orders/account/paper)不得引用即時層、
峰值只由記帳更新(即時價不得墊高它)、過期價格不得回傳、
串流價格優先於 REST 退路。

**2026-09-13 補的四件事(都不改變交易行為)**:

  · **相關性集中度**(第六十條)。總曝險 49% 只是加總,它不會告訴你
    七個倉是不是同一個賭注。算 √(wᵀRw) 與有效持倉檔數 ——
    相關性 1 的時候,七個 7% 的倉等於**一個 49% 的倉**。
    **上限是 None,而那不是「不限制」**:一條 Risk Limit 不該由程式
    自己決定(第 102 條),而我算不出依據。檢查每天照跑、數字照記,
    面板看得到 —— 沒有上限的期間正是最需要看到那個數字的時候。
  · **事件日曆**(第五十一條)。只做看得見,不閃避:擋單會改變進場
    日期,那是不同的報酬序列。日期在版控裡(`docs/events.json`),
    來源與查證日期都標在檔案裡:FOMC 2026–2027 來自聯準會、
    CPI 2026 來自 BLS。**2027 的 CPI 沒有寫** —— 查證當天 BLS 還沒
    發布,而用「通常是第二週」推測出來的日期會讓人以為有在看。
    有一條測試擋住之後有人順手補齊。
  · **限流器**(第四十八條)。四支 systemd 服務各跑各的行程,
    所以令牌桶用檔案鎖跨行程共用。

    **2026-09-13 被交易所打臉一次,已從 5 rps / 突發 10 下修為
    2 rps / 突發 3。** 對帳的逐幣持倉查詢在兩秒內打了八個請求,
    BingX 回 429。學到兩件事:**突發才是問題,不是平均速率**
    (「同一端點連續十發」正是觸發逐端點限制的形狀,平均下來明明很慢);
    而 ccxt 的 10 rps 是**全域**參考值,交易所還有沒公開的**逐端點**
    限制,一個全域桶模型化不了它。
    接下單端點時要給它**獨立的桶** —— 行情把桶抽乾導致平倉單送不出去,
    是這裡最不能接受的失敗。
    測試掃 AST 擋住新的繞道:一個可以繞過的限流器只是裝飾品。
  · **/health**(第六十六條)。巡檢的輸出是給人看的,而
    「依賴會有人通知我」的監控,壞掉的時候剛好也不會通知你。
    任何一條不過就回 503,**沒有「大部分還好」這個選項**。

同一天刪掉 `models/signal.py`(160 行,舊系統的 Agent 契約化石,
沒有任何檔案 import 它),並修掉 `scripts/daily.py` 一個只有
Python 3.12 讀得懂的 f-string —— 在 3.11 上它是載入時 SyntaxError,
整支記帳腳本一行都跑不到。

`scripts/daily.py` 每日 00:30 UTC 串起來跑一次
(`agmcis-portfolio.timer`),`scripts/dashboard.py` 是面板,
`scripts/sentinel.py` 每 10 分鐘巡檢,`scripts/gauge.py` 收費率快照
(成本模型的實測來源),`scripts/archivist.py` 備份。

**沒有常駐迴圈。** 常駐進程會讓「程式改了但沒生效」變成無聲的錯誤 ——
2026-09-07 就是這樣讓四天的改動一行都沒跑,而測試全過、面板正常、
哨兵全綠,沒有任何一條檢查會發現。

**跑測試前先跟交易所要一次資料**,否則會看到 23 條紅字:

```
python -c 'from portfolio.specs import refresh; refresh()'
python -c 'from portfolio.specs import refresh_funding; \
           from portfolio.paper import SYMBOLS; refresh_funding(SYMBOLS)'
```

`data/` 有 gitignore(快取不該進版控),所以乾淨的機器上那些檔案
不存在。`tests/conftest.py` 會把因此失敗的測試改判成 skip 並附上
這兩行 —— **skip 不是綠燈**,它只是把「環境沒裝好」與「程式壞了」
分開。這個 repo 需要 **Python 3.12 以上**。

### ⚠ 波動目標算的是「籃子」,不是「持倉」(2026-09-13 釐清)

`vol_target` 一律拿**全部七個幣的等權籃子**去算已實現波動
(`paper.py:203` 永遠傳完整的 `syms`),不管當天實際持有幾檔。

這不是 bug,是一個沒有寫下來的設計選擇,而且它有兩個重要後果:

  · **相關性已經在部位大小裡了。** 籃子的報酬序列本身就含相關性 ——
    相關性升高 → 籃子波動升高 → 縮放係數變小 → 自動減碼。
    所以第六十條的相關性門檻**不是**用來管「相關性造成的波動」,
    那件事已經有人在做。它管的是另一件事:**同時被清算的風險**
    —— 2026-09-10 那次六個倉一起貼在強平線上,波動目標對它無能為力。
  · 量的是「市場波動」而不是「組合波動」,所以它**穩定**:
    不會出現「持有變少 → 量到的波動變低 → 加更多槓桿」的回饋圈。
    代價是持倉集中時實際波動會高於目標;實測影響在相關性高的
    時候很小(ρ=0.8、7 檔剩 1 檔約高 10%),而相關性高正是
    最需要它準的時候。

`vol_target` 的 docstring 原本寫「把整個組合的規模調到估計年化波動
= 目標」—— 那句話描述的不是程式在做的事。

## 二、策略與它的證據

50 日均線曝險 + 波動目標,3.3 年日線(2023-05-25 → 2026-09-06、
1201 個交易日、7 幣、成本全扣):

| | 總報酬 | 年化 | 最大回撤 | Calmar | Sharpe |
|---|---|---|---|---|---|
| 買入持有(基準) | 91.4% | 26.8% | **66.9%** | 0.40 | 0.69 |
| **50 日均線 + 波動目標 27%** | 76.5% | 23.1% | **17.4%** | **1.33** | 1.23 |

唯一訓練段與驗證段**都**贏過基準的(2.81 vs 1.85;0.33 vs -0.42)。
**但回撤 17.4% 仍未達契約 15%,而我沒有把目標再往下調到剛好過** ——
那是拿驗證段的答案調參數,契約存在的理由就是擋這個。

必須一起讀的三個保留:**六選一**(多重檢定)、
**倖存者偏誤**(7 幣都是今天還活著且有 3.3 年歷史的)、
**只有一個市場週期**。

### ⚠ Calmar 1.33 是上限,不是期望值(2026-09-09 實測)

倖存者偏誤不只是一句保留,它的規模被量出來了。用**逐時點篩選**
(每一天只用該日之前的成交量決定交易池,無後見之明)跑同一條策略:

| 訓練段 | Calmar |
|---|---|
| 現役 7 幣(後見之明挑選) | **2.06** |
| 逐時點動態交易池(無後見之明) | **0.00** |

差距不是曝險造成的(實測動態池曝險是現役的 71%,而 Calmar 是
年化÷回撤,等比縮放時幾乎不變)。另一個獨立證據指向同一件事:
**現役 7 幣籃子的買入持有基準訓練段 +194%,21 幣籃子只有 +85.8%**——
那 7 個幣本身就是這段期間表現特別好的一批。

**所以 1.33 應該當作上限看,真實期望值可能明顯更低。**
這不推翻策略——動態池版本在兩段都仍勝過**它自己的**基準,策略本身
有效;改變的是我們該對這個數字抱持多少信心。

完整結果與四項限制:`docs/result-dynamic-universe-and-xsmom.md`

### 策略真正的價值在哪裡(同次實測,現役 7 幣、共同區間)

| | 策略 | 買入持有 |
|---|---|---|
| 訓練段(牛市) | +57.99% | +148.03% |
| 驗證段(熊市) | **+9.75%** | **−28.28%** |
| 全期間最大回撤 | **16.15%** | **65.35%** |

**兩個區段都賺錢**,而買入持有做不到。代價是強牛市大幅跑輸——
規則就是跌破均線出場,所以每次回檔都會被洗掉一段。
**它用上漲的一部分,換掉四分之三的回撤。** 這是這條策略的全部意義,
不是它的缺陷。

## 三、鐵則

1. **不要有第二份實作。** 舊系統兩天內犯了十一次「兩把尺」——
   同一個概念兩份實作,每一次都把負的算成正的。
   `tests/test_portfolio_parity.py` 逐項斷言前向與回測共用同一個函式。
2. **訊號與成交之間永遠隔一個可交易的間隙。** 第 i 日收盤決定,
   第 i+1 日開盤成交,算到第 i+2 日開盤。
3. **成本只有一個來源**(`portfolio/costs.py`),誰都不准自己寫常數。
   含**持有永續每年 5.58% 資金費** —— 基準也是永續,也付這筆。
4. **出場線 = 策略本身的規則**(50 日均線),不是另挑的百分比。
   另挑一個等於偷偷加一個沒被回測過的參數。
5. **監控層不自動調參數。** 那是把過擬合自動化。它只量
   「行為還是不是回測時的那條策略」,跑出範圍就講,停不停由執政官裁決。
6. **改前備份、改後 `py_compile`、收尾 `git commit`。**
7. **中文路徑加引號**;`data/` 與 `.env` 不入 git。

## 三之二、走向自動下單的路線(2026-09-13 立案)

執政官的目標:**能自動判斷並下單、在標準合約上獲利的輔助工具。**
2026-09-13 執政官裁定:「那就按照標準合約去做,之前的直接改掉。」
這是那條路,以及每一步為什麼要照這個順序。

**先決:選哪個產品。** 標準合約有兩種,只有一種能自動下單 ——
U 本位(`contract/v1`)沒有下單端點,幣本位(`cswap/v1`)有。
細節見下面「標準合約有兩種」。

```
✅ A 唯讀私有端點      看得到帳戶            exchange/bingx/private.py
✅ B 對帳層            兩本帳對得起來        portfolio/reconcile.py
✅ C 下單層 + 後備停損  三道閘,預設乾跑       exchange/bingx/trade.py
✅ H 標準合約兩產品分清  U 本位選定           exchange/bingx/standard.py
✅ I 強平價我方算        線性合約,公式沿用      standard_usdt.py
✅ J 精度從成交史反推    沒有 contracts 端點     standard_usdt.infer_spec
✅ K 下單端點定案       **沒有** —— 對照組證實    probe_ustd_order.py
✅ L 指令單 + 執行對帳   最後一吋是人按的        portfolio/ticket.py
✅ P 對齊單             真實帳戶追上模擬        ticket.catch_up()
✅ Q 研究迴路每日自動跑  提案,不是改動          daily.py ④
   M 停損百分比         證據已備,§102 你決定    stop_evidence.py
   N 交易池             這個產品有哪些幣?       discover_universe.py
   O 成本               費率/資金費/滑點未驗證   costs.STANDARD_COSTS_VERIFIED
   E Kill Switch       一鍵停止並平倉(手動)   §47
   G 實盤最小額         人工簽署,契約八條
```

**M / N / O 是上線前必須關掉的三個洞**,而它們都不是程式問題:

  · **M** 停損擺哪裡是 Risk Limit(§102)。`scripts/stop_evidence.py`
    把歷史證據算出來 —— 均線出場觸發前最深的逆向走勢、每個停損位置
    會掃掉幾段、其中有幾段其實是賺的。**數字有了,決定還是你的。**
  · **N** 這個產品**沒有 contracts 端點**,程式問不到有哪些幣可以交易。
    而 App 截圖上看得到的十個裡面**沒有 SOL 與 BNB** —— 那是策略
    現役七幣裡的兩個。若真的沒有,交易池要改,而交易池一改,
    回測的 Calmar 1.33 就不是這個池子的數字了,要重跑(§6)。
  · **O** costs.py 的每一個數字都是量**永續**量出來的。標準合約的
    手續費、資金費、滑點**一個字都沒驗證過**。不要因為「都是 BingX、
    都是 U 本位」就沿用 —— 那正是舊系統「兩把尺」的形狀。

### ⚠ 停損不可以掛在均線上(2026-09-13)

直覺會想:策略出場是 50 日均線,那就在均線那個價位掛一張停損單。
**那是錯的,而且錯得很安靜。**

回測的規則是「**收盤**跌破均線,隔日開盤出場」。掛單的規則是
「**盤中任何一刻**觸到就成交」。加密貨幣一天插針兩三次是常態 ——
把停損掛在均線上,會在無數個「盤中破線、收盤拉回」的日子被掃出去。
那是另一條策略,而 **Calmar 1.33 不是它的數字**。

所以交易所端的停損是**災難後備**,不是策略出場:它擺在策略正常運作
時永遠碰不到的地方,只回答一個問題 —— **這台機器死掉的時候,
這個倉最多能虧多少?**

### ✅ 後備停損 = 25%(2026-09-13 執政官裁定)

`BACKSTOP_PCT = 25.0`。證據來自 `scripts/stop_evidence.py`,
50 日均線、7 幣、**260 段持有**:

| 停損 | 掃掉 | 占比 | 其中最後是賺的 |
|---|---|---|---|
| 8% | 73 | 28.1% | 4 |
| 15% | 20 | 7.7% | 2 |
| **20%** | 13 | 5.0% | **0** |
| **25%** | 5 | 1.9% | **0** |
| 30% | 2 | 0.8% | 0 |

**20% 是分水嶺**:從那裡開始,被掃出去的部位沒有一段最後是賺的。
歷史上凡是逆走 20% 以上的,結局都是虧的。

**選 25% 不選 20%**,因為這是**災難後備**不是策略出場 —— 它該擺在
策略正常運作時永遠碰不到的地方。20% 會在 5.0% 的持倉上觸發
(大約每二十次進場碰一次,太常);25% 只在 1.9% 上。

**上限那一側**:3× 的強平距離約 32.8%,而**那是我方算的、偏樂觀的**
(MMR 取定值,未計維持保證金分層)。25% 留 7.8pp 給誤差;
30% 只留 2.8pp,太薄。

決定的全文(誰、何時、依據、**已知限制**、何時該重新檢視)記在
`exchange/bingx/trade.BACKSTOP_DECISION`,有測試檢查那五樣都在 ——
**一個沒有出處的數字,跟一個程式自己挑的數字,事後是分不出來的。**
而且測試要求 `known_limits` 裡必須有「歷史沒有上限保證」這句:
一條只寫好處的 Risk Limit 會讓下一個人以為它沒有代價。

已知限制:歷史最深是 UNI 36.7% / SOL 36.3%(25% 掃得掉,但那兩段
最後都是虧的);BTC 與 BNB 歷史最深只有 11.1% / 12.1%,25% 對它們
非常寬;而這個數字是拿**永續**的日線算的,標準合約的簿子沒量過。

### 2026-09-13 第一次送 Demo 單:code=100004

拒單原因是**權限**,不是簽章、不是路徑、不是參數 —— 它一路過了認證、
路由與參數結構檢查,卡在最後一道權限閘(金鑰只勾了讀取)。

**那是好消息**:唯一還沒驗證的參數鍵名,大概率是對的。

### ⚠ API 的 VST 帳戶,不是 App 裡的那個 Demo 帳戶(2026-09-13 實測)

執政官在 App 的 Demo 裡開了一個 AVA-USDT 的倉。API **完全看不到它** ——
整批查、逐幣查、指名查 `AVA-USDT`、標準合約、實盤主機,全部 0 筆。

不是端點問題,是**兩個不同的錢包**:

| | API(`open-api-vst`) | App 的 Demo |
|---|---|---|
| 權益 | **剛好 100,000.00** | 121,479.1 |
| 已用保證金 | **0.0000** | 12,147.91 |
| 持倉 / 掛單 | 0 / 0 | 1 / 0 |

API 那個 VST 帳戶是**全新未動過的** —— 剛好初始額度、保證金零、
連歷史掛單都沒有。它是第一次呼叫 API 時才被開出來的。

**後果:手動在 App 開倉無法用來驗證 API 的欄位形狀。**
要在 API 的 VST 帳戶裡產生持倉,只能讓程式自己下單 ——
也就是必須給金鑰交易權限。

(實盤帳戶同時確認:權益 0.05 USDT、0 持倉、0 掛單 ——
沒有真錢在冒險。)

### ⚠ 「標準合約」有兩種,而它們不是同一件事(2026-09-13 定案)

執政官從一開始要的就是標準合約。App 上有兩個分頁 ——
**U本位標準合約**與**幣本位標準合約** —— 在 API 上是兩個完全不同的產品:

| | U 本位標準合約 | 幣本位標準合約 |
|---|---|---|
| API | `/openApi/contract/v1` | `/openApi/cswap/v1` |
| 代號 | `BTCUSDT`(無槓) | `BTC-USD`(有槓) |
| 端點數 | **3 個,全是 GET** | 17 個 |
| 下單 | **沒有這個端點** | `POST .../trade/order` |
| 附帶停損 | — | **有**(下單時直接帶 stopLoss) |
| 強平價 | **回應裡沒有** | `liquidationPrice` 有 |
| 結算 | USDT(正向) | **以幣結算(反向)** |
| 資金費 | 未查證 | **有**(premiumIndex) |
| 到期日 | 未查證 | **沒有**(官方稱 perpetual) |

依據:官方 `BingX-API/BingX-Standard-Contract-doc` 的 REST API 全文只有
allPosition / allOrders / balance 三個 GET,並自陳 "currently in internal
testing";官方 `BingX-API/api-ai-skills` 的 `cswap-trade` 則列了完整 17 個。

**2026-09-13 執政官裁定:「那就做U本位」「補充做標準合約U本位」。**
系統照 U 本位標準合約做。它的下單端點還沒定案(見下),
但**除了送單以外的每一層都做得起來,而且比幣本位好做**。

### U 本位標準合約:少三格,而三格都補得回來(2026-09-13)

`/openApi/contract/v1` 的回應**結構性地**少了三個欄位 —— 官方欄位表
就沒有,不是這次沒回到:

| 少的 | 為什麼要緊 | 怎麼補 |
|---|---|---|
| `liquidationPrice` | 風控的強平距離下限是硬閘(§19) | **我方算**,見下 |
| `markPrice` | 強平用標記價判定,不是最新價 | 只有 `currentPrice`,有誤差 |
| `equity` | 對帳把它列為 critical | `balance + crossUnPnl` 是推的 |

處理方式是第三種答案:**不報成「缺」,也不報成「有」,報成「由我方補」。**
`portfolio/reconcile.py` 的 `KNOWN_ABSENT` 登記這件事,
`FieldReport.fully_from_exchange` 讓「對得上」與「全部是交易所說的」
變成兩個不同的問題。

### ✅ 它是線性合約,所以帳本一行都不用改(2026-09-13 實測)

```
FLOCKUSDT SHORT  3911.11 張  開倉 0.08088  20×  逐倉
名目 3911.11 × 0.08088 = 316.33
316.33 ÷ 20            = 15.82
交易所回的 initialMargin = 15.84   ← 對得上
```

**保證金 = 名目 ÷ 槓桿**,這就是線性(USDT 保證金)的定義。
`portfolio/account.py` 那條已核對過的強平價公式直接適用:

    多單 = 開倉價 × (1 − 1/L + MMR)      空單 = 開倉價 × (1 + 1/L − MMR)

這是 U 本位相對幣本位**最大的優勢**:幣本位是反向合約,盈虧對價格
非線性,account.py 與 paper.py 每一條算式都得重寫。U 本位不用。

### ⚠ 我方算的強平價**偏樂觀**,而那件事要看得見

`StandardPosition.liq_source` 是 `"computed"` 時 `liq_optimistic=True`。
原因:MMR 取 account.py 的 0.005 定值,而交易所有**維持保證金分層**
—— 大倉位的 MMR 更高,實際強平價會比我們算的**更近,不會更遠**。
另外有沒有追加過保證金、有沒有全倉共用,我們看不到。

所以它只能用來**收緊**判斷,不能用來放行。

### ⚠ 沒有 contracts 端點:精度從成交史反推,而那是**下界**

2026-09-09 的教訓是「不知道精度就會產生七張全部被拒的單」。
`contract/v1` 查不到精度、最小下單量、費率 —— 但 `allOrders` 有真實
成交:`executedQty` 反推數量精度、`avgPrice` 反推價格精度。

⚠️ 看到最細 4 位,只代表**至少**支援 4 位 —— 也許只是從來沒人下過更細的。
所以它拿來**擋掉明顯不合規的單**可以,拿來當四捨五入的依據**不行**。
沒有成交史就回 `None`,**不套用其他標的的精度**。

### ✅ 定案:U 本位標準合約沒有下單 API(2026-09-13 實測)

```
/openApi/contract/v1/order        GET 100400   POST 100400
/openApi/contract/v1/trade/order  GET 100400   POST 100400
/openApi/contract/v1/allOrders    GET code 0   POST 100400
對照組 cswap/v1/trade/order        POST 100004(存在,只缺權限)
```

**對照組的 POST 問得出東西**,而 `allOrders` 的 GET 回了 code 0 ——
所以「全是 100400」不是我用錯 HTTP 動詞,是真的沒有這個端點。
這一次的結論站得住,因為它有對照組。

### 於是:最後一吋是人按的

```
訊號 → 部位大小 → 風控閘 → 強平距離 → 指令單 → 人按 → 對帳
 自動    自動        自動      自動       自動     手動   自動
```

`portfolio/ticket.py`。一張指令單不是「建議」,是**一張有有效期的合約**,
因為手動下單有三個會出人命的坑:

一、**價格會動。** 08:00 算出來的量,14:00 按下去是另一筆交易。
    每張單帶 `valid_until`(預設 30 分)與 `price_band`(預設 ±1%),
    **超出就作廢,不是「參考一下」** —— 部位大小整套邏輯是建立在
    那個報價上的。
二、**人會按錯,而錯了沒有人會發現。** 按錯數量、按錯方向、忘了改
    槓桿、忘了改保證金模式 —— 四件事沒有一件會自己發出聲音。
    `--verify` 拿交易所實際的持倉回頭比對,差一格就報出來。
    **只報告,不修正**(§17)。
三、**人會按兩次。** `ticket_id` 是確定性雜湊,同一個決定重算一百次
    是同一個 id —— 對帳時看得出這是同一張單被執行了兩次。

發單前的兩道硬閘,任何一道沒過就**不印半張**:
  · 停損必須在強平**之前**(擺在後面等於沒有停損)
  · 開倉當下離強平 ≥20%(§19),而且那個距離**是偏樂觀的**

`--stop-pct` **沒有預設值**(§102)。決定它需要證據:回測裡均線出場
真的觸發之前,單一部位最深的逆向走勢是多少?

```bash
.venv/bin/python scripts/ticket.py --stop-pct 8            # 今天要按什麼
.venv/bin/python scripts/ticket.py --stop-pct 8 --check    # 現在還能不能按
.venv/bin/python scripts/ticket.py --stop-pct 8 --verify   # 按完對帳
```

### ⚠ 這個產品的成本一個字都沒驗證過(2026-09-13)

`portfolio/costs.py` 的 `TAKER_FEE_PCT = 0.05` / `SLIP_FLOOR_PCT = 0.02` /
資金費年化 7.12%,**全部是量永續量出來的**。

  · **手續費** `contract/v1` 沒有 commissionRate 端點,問不到
  · **資金費** 標準合約收不收,沒有人查證過。官方文件的
    ACCOUNT_UPDATE 事件列表裡有 `FUNDING_FEE`,但那一整段是從
    **現貨**文件複製過來的(dataType 寫的是 `spot.executionReport`),
    不能當證據
  · **滑點** 標準合約有自己的簿子,深度未測

`STANDARD_COSTS_VERIFIED = False`,而 `standard_cost_caveat()` 會把
這句話印在每一份指令單旁邊,直到有人真的量過為止。

問得出答案的地方:`allOrders` 回真實成交的 `cumQuote` 與
`executedQty`,兩者比對得出實際成交價,再跟當時的標記價比就是滑點。

### ⚠ 交易池:同一支腳本錯了兩次,而第二次是自己打自己的臉

`contract/v1` **沒有 contracts 端點**,「有哪些幣可以交易」只能繞著問。
我繞了兩次,兩次都給出一個聽起來很確定的錯答案。

**第一版**比**回傳筆數**:

```
FLOCKUSDT 回 12 筆    ZZZZZUSDT(我編的)回 49 筆
✓ 兩者回應不同 —— 分辨得出        ← 錯
```

12 ≠ 49,所以判別式說「分辨得出」,然後對 14 個候選全部印「存在」。
真相是交易所**認不得 symbol 就不套用過濾**,直接回整本。

**第二版**改成比訂單編號的集合(這一步對了),但又多做一件事:
從那 49 筆讀 `symbol` 欄位,並宣稱「這是交易所自己列出來的,不是推論」。
它印出來的是:

```
裡面有 1 個不重複的標的:ZZZZZUSDT  49 筆
策略現役七幣裡訂單史上沒出現過的:… BTCUSDT …
```

而 **`BTCUSDT` 在同一份輸出裡回了 4 筆有成交史**。兩行當場互相打臉,
腳本沒發現。真相:回應的 `symbol` 欄位是**把請求參數抄回來的**,
不是那筆訂單真正的標的。

**第七、第八次同一個形狀。** 第二次更糟的地方是:證據就在螢幕上,
而判別式沒有去看它。所以第三版多了一件事 —— **自我打臉檢查**:
任何候選有成交史卻沒出現在倒出來那本的清單裡,就當場宣告
symbol 欄位不可信,停止推論。

**目前確定的**:14 個候選(含策略現役七幣)`allOrders` **都認得**,
過濾都生效。基準 49 筆裡候選只認領了 16 筆,**還有 33 筆屬於我們
沒猜到的標的** —— 這個產品上不只這些幣。

**仍然不確定的**:「allOrders 認得」是**必要條件,不是充分條件** ——
它很可能拿全交易所的代號表做驗證,而不是這個產品的清單。
SOL / BNB / AAVE / UNI 能不能在 U 本位標準合約上交易,**還是未知**。
完整名單只能在 App 上翻。

### 如果哪天要「自動送單 + U 本位」,只有一個產品做得到

**U 本位永續合約**(`/openApi/swap/v2`):有完整下單 API、線性 USDT
保證金(帳本一行都不用改)、七個現役幣種都在。

它不是標準合約,所以現在不是選項 —— 但這句話寫在這裡,是因為
「自動送單」與「U 本位標準合約」在 BingX 上**互斥**,而那不是一個
可以靠寫程式繞過去的限制。哪天要換,換的是產品,不是程式。

### ⚠ 2026-09-10 為什麼會把整個標準合約市場判死

留下這一條是因為它的**形狀**會再出現:

一、`cswap/v1/market/contracts` 回 `104414`,被讀成「端點不存在」。
    104414 的意思是**參數不對**。帶對參數重問,交易所回了 20 檔。
二、U 本位的三個限制(沒下單、沒行情、internal testing)被套用到
    「標準合約」這四個字上,連帶把另一個產品也判掉了。
三、後續的探測**全部用 GET**。而 `cswap/v1/trade/order` 用 GET 問會回
    `QueryOrderRequest.Symbol required` —— GET 是查詢動詞,POST 才是下單。
    用錯動詞得到的 404,不能拿來證明端點不存在。

同一個形狀:**一個聽起來很確定、但其實沒有被完整問過的答案。**
這已經是這個專案第六次記錄它了。

### ⚠ 幣本位「標準合約」其實是永續(2026-09-13)

官方文件的標題是 "Coin-M **perpetual** contracts"。它沒有到期日、會收
資金費(premiumIndex 實測 `lastFundingRate` = 0.000071)。

「標準合約」是 BingX 在 App 上的**產品線名稱**,不是合約性質的描述。

`exchange/types.py` 已經據此改掉:`MarketType` 不准再拿來推論有沒有
資金費、有沒有到期日。`Contract.funding` 是 `None` 時 `has_funding`
會**拋 `Unverified`,不回 False** —— 一個推導出來的錯答案,會讓回測
少扣一筆持續性成本,而那種偏差不會有人發現。

### 幣本位還不能下單,而卡住的是**我們**(2026-09-13)

交易所那邊是通的。擋住的三件事都在我方:

一、**反向合約帳本**。幣本位以幣結算,盈虧 = 張數 × 面額 ×
    (1/開倉價 − 1/平倉價),對價格是**非線性**的 ——
    做空的上檔損失無上限,跟正向剛好相反。
    `portfolio/account.py` 與 `paper.py` 每一條算式都是正向的。
二、**一張等於多少 USD 沒有定案**。文件說 `minTickSize` 是「最小合約
    價值(USD)」,BTC-USD 給 10;拿 ticker 回推是 ~100。差十倍。
    回推本身建立在「quoteVolume 以幣計價」這個文件沒寫的假設上,
    所以它是疑點不是結論 —— 定案要在 Demo 開一張最小單量出來。
三、**金鑰沒有交易權限**(`code=100004`),而開權限前 IP 白名單要先設。

三件都是我們自己能做的工作。這跟「等交易所開放」是完全不同的處境。

### 改用幣本位 = 換掉交易池,那是策略層的決定

幣本位只有 20 檔標的,而且代號是 `-USD`。策略現役的七個幣要一個一個
確認在不在裡面 —— `scripts/standard_report.py` 會列出來。
少掉任何一個都不是介面層可以自己換掉的(第六條)。

### ⚠ Demo 與實盤共用同一把 API 金鑰

給金鑰交易權限,它在**實盤**也能下單。擋住它的是三道鎖:

  一、`.env` 的 `BINGX_ENV=demo`
  二、`LIVE_ENABLED = False`(原始碼常數,Trader 連建立都會失敗)
  三、提款權限 OFF

前兩道是我方的。**金鑰外洩時它們保護不了你 —— 對方不需要用你的程式。**
所以給交易權限的同時必須加 **IP 白名單**綁這台機器。

**只是要驗證持倉欄位的話,完全不需要交易權限** ——
在 BingX 的 Demo 介面手動開一個最小的倉就好。那把兩個決定分開了:
「我要驗證欄位形狀」與「我要讓程式自己下單」是兩件事,
後者值得單獨想清楚,不該被前者順便帶過去。

### 下單層的三道閘

一、`LIVE_ENABLED` 是 False 的時候**只能對 Demo 下單** ——
    連 `BINGX_ENV=live` 都沒有用。環境變數是設定,LIVE_ENABLED 是
    原始碼常數,改它要 commit + 部署(第 45 / 78 條)。
    建構時檢查一次,**送出前再檢查一次**(兩者之間可能隔很久)。
二、`confirm=True` 才會真的送。預設乾跑,把要送的東西完整印出來。
三、Risk Engine(第十九條)。

冪等鍵(第十六條)由「策略 + 標的 + 訊號日 + 用途」雜湊而成,
是**確定性的**:同一個決定重跑一百次產生同一個 id。所以送出失敗時
可以安心去查證 —— 而錯誤訊息說的是「**狀態未知**,交易所可能已經
收到」,不是「失敗」。當成失敗然後重送,就會有兩個倉。

後備停損一律 `reduceOnly`。沒有它的話,一張在倉已經被平掉之後才觸發
的停損單會**開出一個反向的新倉**,而那個倉沒有人在管。

**A 的結果(2026-09-13 實測 Demo)**:簽章通過,VST 100,000,
餘額 / 持倉 / 掛單 / 標準合約餘額四條唯讀路徑全通。

餘額端點是 **`/openApi/swap/v3/user/balance`** —— **v3 不是 v2**。
程式沒有寫死,是兩個都問、讓交易所選。照文件記憶寫死的話現在會是 404。

**F 被移到 E 之前,理由是欄位形狀**:Demo 帳戶有 0 個持倉,所以
`account.py` 宣稱的持倉欄位(`positionAmt` / `avgPrice` /
`liquidationPrice` / `markPrice`)**一個字都還沒被驗證過**。
`reconcile.check_fields` 在沒有樣本時回報「未驗證」而不是「通過」
—— 而那組欄位裡有強平價,算錯的後果不是數字難看,是倉沒了。
要驗證它,需要一個真的倉,哪怕只是 Demo 裡最小的一筆。

**A 已完成,而且刻意只做唯讀。** `private.py` 裡沒有下單的程式碼 ——
不是被擋住,是根本沒寫;有一組測試會在有人加進去的時候變紅。

為什麼順序是這樣:

  · **看不到帳戶就下單,等於閉著眼睛開槍。** A 跑通,整條認證 / 簽章 /
    解析的路也就跑通了 80%,而全程沒有一毛錢的風險。
  · **C 排在下單之前**,因為現在的停損是「跌破 50 日均線就出場」——
    那是一條**規則**,靠這台機器活著才成立。機器掛了、網路斷了、
    程式當了,交易所那邊**什麼保護都沒有**。紙上交易看不出這件事,
    實盤第一次斷線就會知道。
  · **D 排在 C 之後**,因為重送一張市價單的代價,遠大於漏掉一次查詢。

`BINGX_ENV` 預設 `demo` —— 連的是 BingX 的 Demo Trading(VST 虛擬 USDT,
十萬額度,行情與真實市場同步)。連實盤必須明確設定,而且即使連上,
A 這一層能做的也只有讀。

金鑰權限:**只勾讀取,提款永遠 OFF**(第十條,沒有例外)。
到 F 之前都不需要交易權限。

## 四、實盤三道鎖

1. `LIVE_ENABLED = False` 是**原始碼常數** —— 不讀環境變數、不讀設定檔、
   不接命令列。一個能被設定檔打開的實盤開關,遲早會被某次
   「先開起來試試」打開。要開必須改碼、commit、部署。
2. **實盤資格契約八條**(`portfolio/contract.py`,門檻 2026-09-06 裁定,
   不得事後放寬):100 個前向交易日 / 報酬為正 / 勝過基準 /
   回撤 ≤15% / 監控無 HIGH 示警 / 單幣貢獻 ≤40% / 單月貢獻 ≤50% /
   連續 3 個完整月正報酬。**目前 2/8。**
3. **人工簽署,永不自動化。**

### 槓桿改為公式浮動(2026-09-08 執政官裁定「不設限 1×,系統自行判定」)
`vol_target()` 原本封頂在 1.0(波動再低也不加碼)。現在對稱延伸:
波動低於目標就加碼、波動高於目標就減碼(原本就有),**沒有新增自由參數**——
浮動 = 同一條波動目標公式算出來的結果,不是自由發揮。

**上限本身不是系統判定的**,是執政官指定的常數,經三輪討論定案 **20×**
(先問 2×、後改 35×、最終 20×)。20× 的強平距離 = 1/20 − 0.5% = **4.5%**——
只要逆向走 4.5% 那一倉就會被強平,寫清楚不代表推薦,是誠實揭露風險。

### ⚠ 2026-09-10:20× 真的爆了,已下修為 3×

**UNI-USDT 被強平。** UNI 從 6.748 跌到 6.155(−8.79%),遠超過 4.5%。
而查下去發現**其餘六個倉全部在懸崖邊**:BNB 距離強平只剩 **0.14%**、
AAVE 0.82%、XRP 2.00%、SOL 2.15% —— 一場連環爆倉就在當天。

**我先前的分析錯在哪裡:** 下面那段寫「實際使用約 0.5×,20× 是虛設的
天花板」——那句話只對了一半。0.5× 是**帳戶層曝險**(拿多少名目),
但**每一倉的 leverage 都被設成 20**,所以逐倉強平距離就是 4.5%。
波動目標限制的是「拿多少名目」,**不是「每倉設幾倍槓桿」**。
兩件事混在一起看,結論就錯了 —— 而錯的那一半剛好是致命的。

**關鍵事實:名目一樣 → 損益一樣。** 槓桿只決定鎖多少保證金與強平距離:

| 槓桿 | 鎖定保證金 | 強平距離 | 名目(=損益) |
|---|---|---|---|
| 20× | 239(2.4%) | **4.50%** | 4,774 |
| 3× | 1,591(16.1%) | **32.83%** | 4,774 |

可用保證金有 9,616 —— **我們完全不缺保證金,20× 省下的那筆用不到**。
所以 20× 是零好處、純風險。實測改為 3× 後 equity 完全不變。

**3× 的三條獨立依據**(不是搜出來的):
· 反推:強平距離須大於單日極端波動(本組合單日最大跌幅約 10~15%),
  3× 給 32.8%,約 2~3 倍安全邊際
· 學術:arXiv 2102.04591 用 BitMEX 永續 2017-2021、43 萬筆 5 分鐘資料
  + 極值理論,算出多單建議 **33% 保證金(3×)** 以達成 1% 每日追繳機率;
  並指出**常態分布假設會低估最適保證金至少 50%**
· 業界:專業交易者方向性交易極少超過 5~10×,多數框架建議 3~5× 起步

**同時修掉兩個 bug:**
一、`liquidation_check()` 原本**只寫 log 不平倉** —— 部位照樣留著繼續虧,
   UNI 虧到 −61.67(投入保證金只有 35.09、ROI −175%)。從那一刻起帳本
   與交易所永久分岔,而且價格反彈時帳本還會「賺回來」,那在現實中
   不可能發生。現在 `Account.liquidate()` 真的在強平價平倉,損失止於保證金。
   判定改用**當日最低/最高價**:強平是盤中觸價就發生的,只看開盤會漏掉
   「盤中破線、收盤拉回」——而那在真實交易所是實實在在被平掉了。
二、日報**完全沒提強平**,照樣寫「✅ 回撤 1.38%」。憲法第十條:
   「一個只報自己好消息的面板,是在幫操作者自我欺騙。」現已加入 💥 區塊,
   且強平事件會落進權益曲線,不只留在 log。

**下面這段是 2026-09-08 寫的,而它正是導致 09-10 爆倉的錯誤分析,
保留原文供對照——不刪掉錯誤,是為了記得它長什麼樣:**

> 「實測:這個上限現在完全不會被觸發。縮放係數 = 27% ÷ 實際年化波動,
> 3.3 年裡這 7 個幣的實際波動從未低於約 34%,係數從未超過 0.78
> (0/819 個交易日 > 1.0)。20× 現在是虛設的天花板……」

**錯在哪:** 那段量的是「縮放係數有沒有超過 1.0」——也就是**名目會不會
超過權益**。它是對的,而且與強平風險**無關**。真正決定強平距離的是
`fill(..., leverage=LEVERAGE_CAP)` 給每一倉設的槓桿,那個數字一直是 20,
每天都在生效。**「這個參數不會被觸發」與「這個參數沒有風險」是兩件事,
我把它們當成同一件。**

BingX 逐幣真實槓桿上限(20×~150×,依幣種與倉位分層)藏在需要 API 金鑰
的私有端點,無法驗證,**未寫入程式碼**——只用查證過的實測值,不用網路
二手資料冒充交易所真實限制。要調高上限逼近交易所限制時,再申請金鑰查證。

前向樣本因這次改動(策略本身變了)歸零重來,舊帳本歸檔於
`data/portfolio_archive/*-pre-leverage20-0908.*`。

## 五、退役的對照組(留下答案,刪掉程式碼)

**2026-09-13 起這台機器只跑一套系統。** 兩個平行對照組都退役了。
它們的程式碼刪了,答案留在這裡。

### 測試組(2026-09-13 退役)

`portfolio/trial.py` —— 動態交易池的平行前向對照。唯一的變因是交易池:
每次記帳重新篩(30 日成交額中位數 ≥1,000 萬、≥60 根日線、可交易、
排除代幣化傳統資產),其餘設定與主城完全相同。

**它要回答的問題:固定 7 幣的漂亮數字裡,有多少是倖存者偏誤?**
那 7 個幣是「今天還活著而且有 3.3 年歷史」選出來的 —— 回測分不出
「動態池比較差」與「固定 7 幣被後見之明美化」,只有前向能回答。

**退役時它還沒有答案。** 預先登記的判準是前向 ≥60 天,而它跑到
2026-09-10 就因為資金費快取沒跟上動態池而死掉,73.7 小時沒記帳。
執政官裁定收斂為單一系統。

**所以那個問題現在是開著的。** 倖存者偏誤還在,而且沒有前向證據
可以量它。誠實的替代方案是**逐時點篩選的回測**(每一天只用該日之前
的成交量重篩),資料上做得到,但那是另一件工程 —— 在做出來之前,
任何用固定 7 幣算出來的數字都要連著這一段一起讀。

### 放養組(2026-09-10 退役)

`/root/agmcis_wild` —— 無規則對照組。

**它的答案早已出來:** 53.6 天 Sharpe **−1.59**、獲利因子 0.55、
勝率 37.7%、最終 −0.63% vs 同期基準 +30.43%。
**拿掉規則沒有產生優勢** —— 這是「規則值不值得」的實證答案。

⚠️ **2026-09-13 發現文件與機器不符**:這裡原本寫「服務已停用、
開機自啟已移除」,而 `systemctl` 顯示 `agmcis-wild.service active running`。
憲法說關了,機器說開著。以機器為準去查,不要以文件為準。

### ⚠ 測試跑得起來,不代表服務跑得起來(2026-09-13)

面板 `agmcis-dash` 從 commit `e4f404a` 起就再也起不來,而**沒有人發現**:

```
File "/root/agmcis/scripts/dashboard.py", line 33, in <module>
    from core import ratelimit
ModuleNotFoundError: No module named 'core'
agmcis-dash.service: Scheduled restart job, restart counter is at 93
```

原因只有一行:`from core import ratelimit` 被排到了
`sys.path.insert(0, 根目錄)` 的**上面**。
`python scripts/dashboard.py` 的 `sys.path[0]` 是 `scripts/`,不是根目錄。

**為什麼 331 條測試全綠:pytest 會自己把 rootdir 放進 `sys.path`。**
所以測試裡 `import scripts.dashboard` 一路順暢,而 systemd 用
`ExecStart=.../python scripts/dashboard.py` 跑的時候必死。

而它壞掉的方式最惡劣 —— **每一層都說一切正常**:

  · systemd 的 `Type=simple` 在行程 fork 出來那一刻就報 `active`
  · `systemctl restart` 看起來成功,`is-active` 回 `active`
  · 舊的行程還握著埠,所以瀏覽器照常有畫面 —— 只是永遠是舊的
  · 唯一知道真相的是 journalctl 裡那個往上跳的 restart counter,
    而沒有人在看它

我因此連續三輪猜錯:先猜「沒重啟」,再猜「埠被占了」。
兩次都是**一個聽起來很確定、但其實沒有被完整問過的答案** ——
第九、第十次。

擋住它的是 `tests/test_scripts_actually_start.py`:

  · AST 掃 `scripts/` 每一支,任何第一方 import 排在
    `sys.path.insert` 之前就紅。它當場又抓出 `audit_exchange.py`
    犯了同一個錯。
  · 而且**真的用 systemd 的方式跑一次面板**(清掉 PYTHONPATH、
    `python scripts/dashboard.py`),等它綁上埠才算過 ——
    靜態檢查會漏掉下一種沒想到的死法。

面板現在啟動時把版本印進日誌第一行,綁不上埠時明說
「已經有另一個行程占著,舊的會繼續服務,所以外面看起來一切正常」。

## 五之二、研究迴路:系統不停地試,但不能自己生效(2026-09-13)

執政官:「讓系統不斷地經過多次的模擬交易之後,發現該怎麼樣調整
會有利於後續的交易的盈利。」

而這件事有一個會殺死它的形狀:**模擬只有一條歷史**。系統試一千種
變化,一定會找到在那條歷史上最漂亮的一組 —— 而它漂亮的原因是
**記住了過去**,不是理解了市場。結果是一條非常好看的模擬曲線,
和一個賠錢的真實帳戶。

所以 `portfolio/research.py` 的每一個設計都在對抗同一件事:

| | |
|---|---|
| **網格預先登記且很小** | 32 種,寫死。加一個維度要改程式碼、進 git —— 那個摩擦是刻意的 |
| **只往下試槓桿** | 2026-09-10 的 20× 真的爆了。`LEV_GRID` 最大值 = 現任 |
| **訓練段挑,驗證段驗** | 沿用 `judge.py` 的預先登記切分,而且是**時序切不是幣種切** |
| **多重比較校正** | p × 試過的次數(Bonferroni)。試 32 次,其中一次看起來贏幾乎是必然 |
| **現任贏平手** | 驗證段優勢要 ≥ 0.10 Calmar。差不多好就不換 —— 換參數本身有成本 |
| **提案不會自己生效** | 裁決與生效是兩步 |

### ⚠ 我第一版的 bootstrap 統計是錯的,而測試抓到了

第一版直接從**原始資料**重抽,但原始資料含有那個效果 —— 所以重抽
出來的差值圍繞著觀察值而不是零,`P(重抽 ≥ 觀察)` 會趨近 0.5。
**那不是 p 值,是一個看起來像 p 值的數字。**

`test_a_clearly_better_challenger_gets_a_small_p` 抓到它:一條每天
都穩定贏 0.4% 的序列被算出 p=0.17。

正確做法是把差值**減掉它自己的平均**再重抽 —— 那條置中的序列才是
「虛無假設」長的樣子。而區塊重抽(20 天)是因為日報酬有自相關,
逐日獨立重抽會打散結構、低估風險、高估顯著性 ——
**而那個方向剛好是「讓提案容易通過」。**

### 裁決 ≠ 生效,而這兩步必須分得開

  · **裁決**(`scripts/decide.py`)留下的是**決定**:誰、什麼時候、
    根據哪一份證據。它只改 `data/proposals.json` 的狀態。
  · **生效**是人去改常數,commit 訊息帶上提案編號。

合成一步的話,事後只看得到「參數是這個」,看不到「誰決定的」——
而風控參數被誰改過,是這個系統最需要看得見的事情之一(§102)。

裁決過的提案**不得被覆蓋**:改變主意要開一個新的決定,不是改掉
舊的那一筆。重跑研究迴路也不會抹掉既有的裁決。

### 沒有提案是好消息

`scripts/research.py` 跑完沒有產生提案,代表現任參數在整個網格裡
站得住。一個每次都能找到提案的引擎,找到的東西平均而言一文不值。

### ⚠ 寫好了,但沒有人呼叫(2026-09-18)

`portfolio/ticket.catch_up()` 有完整實作、**六條測試**、詳細的 docstring
—— 而全倉庫沒有任何地方呼叫它。寫好五天沒人發現。

後果不是「少一個功能」,是**面板上少了一句真話**:模擬持有 7 個倉、
真實帳戶 0 個倉,而指令單只會說「今天沒有要按的」。那句話是真的,
也是誤導。

**單元測試擋不住這件事,而且它讓情況更糟** —— `tests/test_ticket.py`
直接 import `catch_up` 來測,測試本身就是呼叫者,所以那個函式
看起來活得好好的。

`tests/test_nothing_is_built_but_unwired.py` 掃 AST:功能清單裡的每一個
進入點,都必須有**產品程式碼**(不含 tests/)用到它。它當場又抓出
`infer_spec` —— 那個是誤報(它被當 callback 傳進去,不在 Call 位置),
而修掉誤報的方式是**把檢查放寬到 Load 位置的 Name/Attribute**,
不是把它從清單裡拿掉。

放寬的代價是同名區域變數會造成漏報。那個方向是安全的:
**漏報讓一條測試變綠,誤報讓一個能用的功能被當成死碼刪掉。**

### 支撐壓力突破:同一個想法的另一種量法(2026-09-18)

執政官問:「不是有就是說利用支撐或壓力、突破假突破、回測去判斷嗎?」

**可以,而且已經做了。** `portfolio/rules.donchian_breakout()`:
突破 N 日高進場、跌破 M 日低出場。20/10 與 55/20 是**教科書值**
(海龜系統一、系統二),不是搜出來的。

「支撐壓力」如果讓人用眼睛畫,每個人畫出來的線都不一樣 ——
那種東西**回測不了**,因為它不是一條規則,是一個習慣。
N 日高/低是它的機械版:過去 N 天沒有人願意用更高的價格買,
那就是壓力。

**它跟 50 日均線是同一件事的兩種寫法**:均線是平滑版(價格穿過
一條慢慢移動的線),Donchian 是離散版(穿過一個明確的歷史極值)。
所以這不是「加一個新想法」,是「換一種量法」—— 而哪一種量得準,
**回測說了算,不是誰講得比較有道理**。它進了 `research.py` 的網格
(47 種),走同一組閘門:訓練段挑、驗證段驗、回撤契約、
多重比較校正。突破類多兩個參數,所以要贏得更明顯才算數。

### ⚠ 突破策略我寫錯三次,而三次都不會報錯

一、**拿今天的高點當今天的壓力。** 那永遠成立,回測會漂亮得不像話。
    `_extreme()` 因此刻意**不含第 i 日自己**。這是突破類最常見的
    前視偏誤。
二、**確認時拿每一天各自的壓力去比。** 那把「站穩」變成「連續 K 天
    都創新高」—— 罕見得多,不是同一件事。
三、**改成比「今天的壓力」,更糟。** 突破那一天的高點隔天就進到視窗
    裡變成新的壓力,線會自己往上跳,**站穩永遠站不上去** ——
    確認功能等於永遠不進場,而它看起來只是「比較保守」。

正解:把線**凍住**在突破當下。那條線被穿過去之後有沒有被收回來,
才是「站穩」的意思。三次都是測試抓到的。

### 假突破怎麼濾,以及它的代價

  · **用收盤價,不用盤中最高價。** 插針穿過去再收回來不算突破。
    這一條**不花任何參數**,而它擋掉的假突破最多。
  · `confirm_bars` > 1(連續幾天站穩)**要花一個參數**,所以預設是 1。
    每多一個參數就多一個過擬合的入口 —— 它該由研究迴路帶著多重
    比較校正去證明值得,不是我先設好。

已知弱點,先說在前面:突破類在**震盪盤**會被反覆掃,靠少數幾波大
行情回本 —— 勝率低、單筆虧損頻繁,心理上比均線難拿住。

### ⚠ 用 A 挑、用 B 檢定 —— 研究迴路第一次跑就抓到(2026-09-18)

第一次實跑的結果:

```
現任 50 日均線     訓練 Calmar 2.27   驗證 0.48
最好的挑戰者 100 日均線  訓練 1.19   驗證 0.65   回撤 12.7%
  ✗ 訓練段贏過現任     1.19 vs 2.27
  ✓ 驗證段也贏過現任   0.65 vs 0.48
  ✗ 多重比較校正      原始 p=1.0 × 47 = 1.0
```

`p = 1.0` 的意思是「根本沒贏」,而閘門同時說它贏了 0.17 Calmar。
**兩句話不可能都對。**

原因:bootstrap 檢定的是**平均日報酬**,而整套挑選的準則是 **Calmar**。
100 日均線的平均日報酬**比現任低** —— 它贏在回撤小。

**一個用 A 挑、用 B 檢定的流程,永遠檢定不到它挑的東西。**
一個靠降低回撤取勝的挑戰者在舊版裡永遠過不了,而這正是低波動策略
最常見的取勝方式。改成直接 bootstrap **Calmar**。

⚠️ 誠實標註:Calmar 是比值又是路徑統計量,**置中虛無沒有誠實的做法**,
所以新版量的是「重抽的歷史裡這個優勢有多常站得住」——
**重抽不確定性,不是尾機率**。它被當 p 值丟進 Bonferroni 是一個近似,
而近似的方向是**偏寬鬆**,所以校正後的門檻是下限不是保證。
硬做一個置中出來,會比說清楚更糟。

### ⚠ 網格裡一半是同一個東西換個名字

同一次實跑:`ma100-vol15-lev2` 與 `ma100-vol15-lev3` 的訓練 / 驗證 /
回撤**完全一樣**,每一組都是。原因是波動目標在 15~35% 時**總曝險
根本碰不到槓桿上限**,所以那個參數是惰性的。

而 Bonferroni 乘的是「試了幾次」。把重複的各算一次,等於**無中生有
地加嚴校正** —— 一個真的有效的改良會因為我把網格寫得太冗餘而被擋掉。
**那不是保守,那是算錯。** `effective_trials()` 用行為去重。

### ⚠ 只印前十名,一整族掛掉跟一整族表現差長得一樣

第一次跑,前十名全是 `ma`,一個 `bo` 都沒有。而我在突破那支寫錯過
**三次** —— 所以「bo 不在榜上」必須分得出是**跑輸**還是**沒跑**。
研究迴路因此改成每一族都印出它最好的那一個,一個都沒跑出來時直接說
「那是 bug,不是結果」。

### ⚠ 連結不帶產品別,而那比「連結是哪個產品的」重要得多(2026-09-18)

我看到分享連結的路徑裡有 `/perpetual/`,就說它是永續頁、標準合約的
網址還沒有樣本。**判斷錯了** —— 執政官澄清:那條就是從標準合約頁面
分享出來的。`/perpetual/` 是 BingX 分享功能的網址格式,不是產品別。

**拿一個字串的長相去推斷它的語意**,這一輪的第二次
(第一次是把 `104414` 讀成「端點不存在」)。

而查清楚之後,真正要緊的事在另一個地方。App 連結解碼出來是:

```
bingbon://trade/detail?coinName=BTC&valuationCoinName=USDT&marginCoinName=USDT
```

參數只有三個,**沒有任何一個欄位說這是標準合約還是永續**。

所以連結能做的只有「把 App 開到這個幣」—— **選哪個產品是 App 決定的,
不是連結決定的**。而同一個幣在兩個產品上的合約規格、槓桿上限、費率
都不一樣:

**指令單的數量是照標準合約算的,下到永續上就是一張算錯的單,
而它會成交。**

所以 `PRODUCT_WARNING` 掛在**每一張**指令單上,而且它說出後果
(「會成交」),不是只說「請注意」—— 一個不講後果的警告會被跳過。

## 六、舊系統的教訓(刪掉程式碼,留下教訓)

**為什麼刪:** 帳本 1391 案、十個積木**全部負期望**、六族依規則蓋棺。
多重檢定:以實測 σ=1.378R 計,預期假陽性約 52 個,而**實際通過
DEV 只有 15 案** —— 實際低於雜訊,意思是那個空間本身為負期望,
在同一個空間加大搜尋只會提高假陽性。

1. **任何績效數字都必須有基準線,而且窗口要對。**
   9/5 用「300 天」算出「大勝 19pp」,改用實際 55 天後變成「輸 36pp」,
   因為崩盤發生在系統誕生之前。報酬必須與最大回撤一起報。
2. **成交價必須是市場真的存在過的價格。** 9/5 事故:以區間中點記帳,
   95.4% 的成交價當下不存在,8218 筆全部作廢。
3. **診斷 100% 的異常時,先驗資料真實性,再談統計解釋。**
   同一個 bug 被判過兩次「不是 bug」,兩次都給了合理的統計解釋。
   **統計解釋永遠找得到一個,那正是它危險的地方。**
4. **旋鈕必須真的有作用。** 面板讀得到 `trailing_stop_pct` 而引擎裡
   出現 0 次 —— 看起來存在、實際無作用的參數,比沒有更糟。
5. **稽核自己也會壞,而且壞得無聲。** 欄位名打錯 → `getattr` 靜靜回 None
   → 那條檢查直接跳過,而它每天回報「未發現異常」。
   **絕不用預設值吞掉錯誤 —— 說謊的稽核比沒有稽核危險。**
6. **「兩把尺」十一次。** 成交價 / 出場順序 / 資金費 / 滑點 9.5 倍 /
   穿越停損 / 最低風報比 / 封鎖期 / 最小部位量 / **成交模型**。
   最後那次最嚴重:影子用限價、法庭用市價,零寬度區間成交 0/681,
   而**所有 SHORT 訊號都是零寬** —— 影子在結構上是純多單的。
   **「兩把尺」不只出現在數值上,更常出現在「哪些訊號變成交易」——
   後者更難發現,因為它不會讓任何數字看起來不合理。**
7. **常駐進程會讓改動無聲失效。** autopilot 四天沒重啟,
   執政官裁定的判準改動一行都沒生效,而 git 乾淨、測試全過、面板正常。
8. **一條天天誤報的檢查等於沒有檢查。** 停用的東西不該再被當成停擺;
   盯著不存在的東西的檢查,會讓人習慣忽略紅燈。
9. **面板說了 52 天假話。** 名冊為空時退回讀相容檔,於是一直寫著
   「★ 執行中」。**在職就是在職,沒有退路可言。**
10. **一個只報自己好消息的面板,是在幫操作者自我欺騙。**
11. **「乾淨退出 0」不代表承諾的事真的發生了。** `scripts/daily.py`
    從建立以來沒呼叫 `load_env()`,systemd 用裸環境跑,`.env` 裡的
    Telegram token 從沒被讀進去 —— 記帳、契約更新照常成功,只有
    `telegram.send()` 靜靜印一行「未設定,略過通知」。**日報從這支
    腳本存在以來就沒有真的送出去過**,而其餘每個環節看起來都正常。
    2026-09-09 改版通知格式時順手發現。

## 七、風格

- 回報用繁體中文,先講結論再講細節
- 誠實優先:數據說不行就說不行,不粉飾、不為了讓執政官高興而樂觀
- 重要改動說明「為什麼」而不只是「改了什麼」
